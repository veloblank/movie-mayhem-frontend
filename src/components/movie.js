class Movie {
  constructor(object) {
    this.title = object.title;
    this.synopsis = object.synopsis;
    this.rating = object.rating;
    this.poster_src = object.poster_src;
  }
}

export default Movie;
